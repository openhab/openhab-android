<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ActionButton">
        <item name="android:layout_width">44dip</item>
        <item name="android:layout_height">44dp</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="WhiteText">
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="SectionButton">
        <item name="android:textColor">@color/sectionswitch_text</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">8dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">14sp</item>
        <item name="android:stretchColumns">0</item>
        <item name="android:paddingStart">8dip</item>
        <item name="android:paddingEnd">8dip</item>
        <item name="android:paddingTop">4dip</item>
        <item name="underlineHeight">2dp</item>
        <item name="underlineColor">?attr/colorAccent</item>
        <item name="textDistanceFromLine">10dp</item>
        <!-- Our background uses theme attributes which doesn't work when inflated as
             a drawable on API < 21. This is why we use that custom attribute which has
             special handling in code. -->
        <item name="backgroundColor">@color/sectionswitch_bg</item>
    </style>

</resources>
