name: Validate Strings
on: [push, pull_request, pull-request]

jobs:
  app-store:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        flavor: [play, playBeta, fdroid, fdroidBeta]
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      - name: Validate App Store Strings ${{ matrix.flavor }}
        run: python3 assets/store_descriptions/generate_and_validate.py
  app:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      - name: Validate App Strings
        run: python3 travis/validate-app-strings.py
